# File: run-all-once-win.yml
# Code: Claude Code
# Review: Ryoichi Ando (ryoichi.ando@zozo.com)
# License: Apache v2.0
# Generated by: run-all-once-gen-win.py

name: All Examples (Windows Native)

on:
  workflow_dispatch:
    inputs:
      instance_type:
        description: 'EC2 instance type'
        required: true
        default: 'g6e.2xlarge'
        type: choice
        options:
          - g6.2xlarge
          - g6e.2xlarge
      region:
        description: 'AWS Region'
        required: true
        default: 'us-east-2'
        type: choice
        options:
          - us-east-1
          - us-east-2
          - ap-northeast-1

jobs:
  run-batch-1:
    name: Run Batch 1 (Windows)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: ${{ github.event.inputs.region }}
      INSTANCE_TYPE: ${{ github.event.inputs.instance_type }}
      BRANCH: ${{ github.ref_name }}
      EXAMPLES: "belt cards codim curtain domino"
      WORKDIR: C:\ppf-contact-solver
      USER: Administrator

    steps:
      - name: Show input parameters
        run: |
          echo "## Input Parameters - Batch 1 (Windows)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Instance Type: ${{ github.event.inputs.instance_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Examples: belt cards codim curtain domino"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Find Windows Server AMI and network resources
        id: setup
        run: |
          echo "Finding latest Windows Server 2025 AMI..."
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters \
              "Name=name,Values=Windows_Server-2025-English-Full-Base-*" \
              "Name=state,Values=available" \
            --query 'sort_by(Images, &CreationDate)[-1].ImageId' \
            --region "$AWS_REGION" \
            --output text)

          if [ "$AMI_ID" = "None" ] || [ -z "$AMI_ID" ]; then
            echo "ERROR: Windows Server 2025 AMI not found in region $AWS_REGION"
            exit 1
          fi
          echo "AMI_ID=$AMI_ID" >> $GITHUB_OUTPUT
          echo "Found AMI: $AMI_ID"

          # Get GitHub Actions dedicated VPC, subnet, and security group
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=github-actions-vpc" --query 'Vpcs[0].VpcId' --output text)
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[0].SubnetId' --output text)
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=github-actions-sg" --query 'SecurityGroups[0].GroupId' --output text)

          if [ "$VPC_ID" = "None" ] || [ -z "$VPC_ID" ]; then
            echo "ERROR: github-actions-vpc not found in region $AWS_REGION"
            exit 1
          fi

          echo "::add-mask::$VPC_ID"
          echo "::add-mask::$SUBNET_ID"
          echo "::add-mask::$SG_ID"
          echo "SUBNET_ID=$SUBNET_ID" >> $GITHUB_OUTPUT
          echo "SG_ID=$SG_ID" >> $GITHUB_OUTPUT
          echo "VPC: $VPC_ID, Subnet: $SUBNET_ID, SG: $SG_ID"

      - name: Generate unique identifiers
        id: ids
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "Timestamp: $TIMESTAMP"

      - name: Create EC2 key pair
        id: keypair
        run: |
          KEY_NAME="win-batch-1-${{ steps.ids.outputs.TIMESTAMP }}"
          echo "::add-mask::$KEY_NAME"
          echo "KEY_NAME=$KEY_NAME" >> $GITHUB_OUTPUT

          aws ec2 create-key-pair \
            --key-name "$KEY_NAME" \
            --query 'KeyMaterial' \
            --output text > /tmp/ec2key
          chmod 600 /tmp/ec2key
          echo "Created key pair: $KEY_NAME"

      - name: Create Windows user data script
        run: |
          # Use standard SSH port 22 for EC2 Instance Connect
          sed "s/SSH_PORT_PLACEHOLDER/22/g" .github/workflows/scripts/win/user-data.ps1 > /tmp/user-data.ps1
          echo "User data script created with SSH port 22"

      - name: Launch EC2 instance
        id: instance
        run: |
          echo "Launching Windows EC2 instance..."
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "${{ steps.setup.outputs.AMI_ID }}" \
            --instance-type "$INSTANCE_TYPE" \
            --key-name "${{ steps.keypair.outputs.KEY_NAME }}" \
            --subnet-id "${{ steps.setup.outputs.SUBNET_ID }}" \
            --security-group-ids "${{ steps.setup.outputs.SG_ID }}" \
            --associate-public-ip-address \
            --user-data file:///tmp/user-data.ps1 \
            --block-device-mappings "DeviceName=/dev/sda1,Ebs={VolumeSize=100,VolumeType=gp3,DeleteOnTermination=true}" \
            --tag-specifications \
              "ResourceType=instance,Tags=[{Key=Name,Value=gpu-runner-win-batch-1-${{ steps.ids.outputs.TIMESTAMP }}},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner},{Key=Workflow,Value=${{ github.workflow }}},{Key=RunId,Value=${{ github.run_id }}},{Key=Branch,Value=${{ env.BRANCH }}},{Key=Batch,Value=1}]" \
              "ResourceType=volume,Tags=[{Key=Name,Value=gpu-runner-win-batch-1-${{ steps.ids.outputs.TIMESTAMP }}-volume},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner}]" \
            --instance-initiated-shutdown-behavior terminate \
            --query 'Instances[0].InstanceId' \
            --region "$AWS_REGION" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT
          echo "Instance launched: $INSTANCE_ID"

      - name: Wait for instance ready
        run: |
          INSTANCE_ID="${{ steps.instance.outputs.INSTANCE_ID }}"
          echo "$INSTANCE_ID" > /tmp/instance_id.txt
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids "$INSTANCE_ID"

          echo "Waiting for OpenSSH to be ready via EC2 Instance Connect..."
          for i in {1..30}; do
            timeout 15 aws ec2-instance-connect open-tunnel \
              --instance-id "$INSTANCE_ID" \
              --remote-port 22 \
              --local-port 2222 &
            TUNNEL_PID=$!
            sleep 5

            if nc -z localhost 2222 2>/dev/null; then
              echo "SSH is responding on attempt $i"
              kill $TUNNEL_PID 2>/dev/null || true
              break
            fi

            kill $TUNNEL_PID 2>/dev/null || true
            if [ $i -eq 30 ]; then
              echo "ERROR: SSH not ready after 30 attempts"
              exit 1
            fi
            echo "Attempt $i/30: SSH not ready, waiting 10s..."
            sleep 10
          done
          echo "Instance ready for SSH connections"

      - name: Install NVIDIA driver
        run: |
          echo "Installing NVIDIA driver..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key .github/workflows/scripts/win/install-nvidia-driver.ps1 Administrator@localhost:C:/install_driver.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/install_driver.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Transfer repository to instance
        run: |
          echo "Creating and transferring repository archive..."
          git archive --format=zip --output=/tmp/repo.zip HEAD

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/repo.zip Administrator@localhost:C:/source.zip

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"if (Test-Path 'C:\\ppf-contact-solver') { Remove-Item -Recurse -Force 'C:\\ppf-contact-solver' }; New-Item -ItemType Directory -Path 'C:\\ppf-contact-solver' -Force; Expand-Archive -Path 'C:\\source.zip' -DestinationPath 'C:\\ppf-contact-solver' -Force; Remove-Item 'C:\\source.zip'\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run warmup.bat
        run: |
          echo "Running warmup.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && warmup.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run build.bat
        run: |
          echo "Running build.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && build.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Setup CI directory
        run: |
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"New-Item -ItemType Directory -Path 'C:\\ci' -Force\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run belt
        run: |
          echo "Running belt..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/belt/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_belt.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_belt.ps1 Administrator@localhost:C:/run_belt.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_belt.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run cards
        run: |
          echo "Running cards..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/cards/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_cards.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_cards.ps1 Administrator@localhost:C:/run_cards.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_cards.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run codim
        run: |
          echo "Running codim..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/codim/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_codim.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_codim.ps1 Administrator@localhost:C:/run_codim.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_codim.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run curtain
        run: |
          echo "Running curtain..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/curtain/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_curtain.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_curtain.ps1 Administrator@localhost:C:/run_curtain.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_curtain.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run domino
        run: |
          echo "Running domino..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/domino/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_domino.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_domino.ps1 Administrator@localhost:C:/run_domino.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_domino.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true


      - name: Collect results
        if: success() || failure()
        run: |
          echo "Collecting results..."
          mkdir -p ci

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          # Delete large binary files on remote before copying to save bandwidth
          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"Get-ChildItem -Path C:\\ppf-contact-solver\\cache\\ppf-cts\\ci -Recurse -Include '*.bin','*.pickle','*.ply','*.gz' -ErrorAction SilentlyContinue | Remove-Item -Force\"" || true

          # Copy CI output from ppf-cts cache directory
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ppf-contact-solver/cache/ppf-cts/ci/*" ./ci/ || echo "No ppf-cts CI files found"

          # Also copy logs and scripts from C:\ci
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ci/*" ./ci/ || echo "No script/log files found"

          echo "## Collected Files:"
          ls -laR ci/ || echo "No files collected"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-win-batch-1
          path: ci
          retention-days: 3

      - name: GPU information
        if: success() || failure()
        run: |
          echo "Getting GPU information..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost "nvidia-smi" || echo "Failed to get GPU info"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Re-authenticate for cleanup
        if: always()
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Cleanup - Terminate Instance
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.instance.outputs.INSTANCE_ID }}" ]; then
            echo "Terminating instance: ${{ steps.instance.outputs.INSTANCE_ID }}"
            aws ec2 terminate-instances \
              --instance-ids "${{ steps.instance.outputs.INSTANCE_ID }}" \
              --region "$AWS_REGION" || true
          fi

      - name: Cleanup - Delete Key Pair
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.keypair.outputs.KEY_NAME }}" ]; then
            echo "Deleting key pair: ${{ steps.keypair.outputs.KEY_NAME }}"
            aws ec2 delete-key-pair --key-name "${{ steps.keypair.outputs.KEY_NAME }}" || true
          fi
          rm -f /tmp/ec2key

      - name: Summary
        if: always()
        run: |
          echo "## Batch 1 Summary (Windows)"
          echo "- Examples: belt cards codim curtain domino"
          echo "- Instance: ${{ steps.instance.outputs.INSTANCE_ID || 'Not launched' }}"

  run-batch-2:
    name: Run Batch 2 (Windows)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: ${{ github.event.inputs.region }}
      INSTANCE_TYPE: ${{ github.event.inputs.instance_type }}
      BRANCH: ${{ github.ref_name }}
      EXAMPLES: "drape fishingknot fitting friction"
      WORKDIR: C:\ppf-contact-solver
      USER: Administrator

    steps:
      - name: Show input parameters
        run: |
          echo "## Input Parameters - Batch 2 (Windows)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Instance Type: ${{ github.event.inputs.instance_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Examples: drape fishingknot fitting friction"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Find Windows Server AMI and network resources
        id: setup
        run: |
          echo "Finding latest Windows Server 2025 AMI..."
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters \
              "Name=name,Values=Windows_Server-2025-English-Full-Base-*" \
              "Name=state,Values=available" \
            --query 'sort_by(Images, &CreationDate)[-1].ImageId' \
            --region "$AWS_REGION" \
            --output text)

          if [ "$AMI_ID" = "None" ] || [ -z "$AMI_ID" ]; then
            echo "ERROR: Windows Server 2025 AMI not found in region $AWS_REGION"
            exit 1
          fi
          echo "AMI_ID=$AMI_ID" >> $GITHUB_OUTPUT
          echo "Found AMI: $AMI_ID"

          # Get GitHub Actions dedicated VPC, subnet, and security group
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=github-actions-vpc" --query 'Vpcs[0].VpcId' --output text)
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[0].SubnetId' --output text)
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=github-actions-sg" --query 'SecurityGroups[0].GroupId' --output text)

          if [ "$VPC_ID" = "None" ] || [ -z "$VPC_ID" ]; then
            echo "ERROR: github-actions-vpc not found in region $AWS_REGION"
            exit 1
          fi

          echo "::add-mask::$VPC_ID"
          echo "::add-mask::$SUBNET_ID"
          echo "::add-mask::$SG_ID"
          echo "SUBNET_ID=$SUBNET_ID" >> $GITHUB_OUTPUT
          echo "SG_ID=$SG_ID" >> $GITHUB_OUTPUT
          echo "VPC: $VPC_ID, Subnet: $SUBNET_ID, SG: $SG_ID"

      - name: Generate unique identifiers
        id: ids
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "Timestamp: $TIMESTAMP"

      - name: Create EC2 key pair
        id: keypair
        run: |
          KEY_NAME="win-batch-2-${{ steps.ids.outputs.TIMESTAMP }}"
          echo "::add-mask::$KEY_NAME"
          echo "KEY_NAME=$KEY_NAME" >> $GITHUB_OUTPUT

          aws ec2 create-key-pair \
            --key-name "$KEY_NAME" \
            --query 'KeyMaterial' \
            --output text > /tmp/ec2key
          chmod 600 /tmp/ec2key
          echo "Created key pair: $KEY_NAME"

      - name: Create Windows user data script
        run: |
          # Use standard SSH port 22 for EC2 Instance Connect
          sed "s/SSH_PORT_PLACEHOLDER/22/g" .github/workflows/scripts/win/user-data.ps1 > /tmp/user-data.ps1
          echo "User data script created with SSH port 22"

      - name: Launch EC2 instance
        id: instance
        run: |
          echo "Launching Windows EC2 instance..."
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "${{ steps.setup.outputs.AMI_ID }}" \
            --instance-type "$INSTANCE_TYPE" \
            --key-name "${{ steps.keypair.outputs.KEY_NAME }}" \
            --subnet-id "${{ steps.setup.outputs.SUBNET_ID }}" \
            --security-group-ids "${{ steps.setup.outputs.SG_ID }}" \
            --associate-public-ip-address \
            --user-data file:///tmp/user-data.ps1 \
            --block-device-mappings "DeviceName=/dev/sda1,Ebs={VolumeSize=100,VolumeType=gp3,DeleteOnTermination=true}" \
            --tag-specifications \
              "ResourceType=instance,Tags=[{Key=Name,Value=gpu-runner-win-batch-2-${{ steps.ids.outputs.TIMESTAMP }}},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner},{Key=Workflow,Value=${{ github.workflow }}},{Key=RunId,Value=${{ github.run_id }}},{Key=Branch,Value=${{ env.BRANCH }}},{Key=Batch,Value=2}]" \
              "ResourceType=volume,Tags=[{Key=Name,Value=gpu-runner-win-batch-2-${{ steps.ids.outputs.TIMESTAMP }}-volume},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner}]" \
            --instance-initiated-shutdown-behavior terminate \
            --query 'Instances[0].InstanceId' \
            --region "$AWS_REGION" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT
          echo "Instance launched: $INSTANCE_ID"

      - name: Wait for instance ready
        run: |
          INSTANCE_ID="${{ steps.instance.outputs.INSTANCE_ID }}"
          echo "$INSTANCE_ID" > /tmp/instance_id.txt
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids "$INSTANCE_ID"

          echo "Waiting for OpenSSH to be ready via EC2 Instance Connect..."
          for i in {1..30}; do
            timeout 15 aws ec2-instance-connect open-tunnel \
              --instance-id "$INSTANCE_ID" \
              --remote-port 22 \
              --local-port 2222 &
            TUNNEL_PID=$!
            sleep 5

            if nc -z localhost 2222 2>/dev/null; then
              echo "SSH is responding on attempt $i"
              kill $TUNNEL_PID 2>/dev/null || true
              break
            fi

            kill $TUNNEL_PID 2>/dev/null || true
            if [ $i -eq 30 ]; then
              echo "ERROR: SSH not ready after 30 attempts"
              exit 1
            fi
            echo "Attempt $i/30: SSH not ready, waiting 10s..."
            sleep 10
          done
          echo "Instance ready for SSH connections"

      - name: Install NVIDIA driver
        run: |
          echo "Installing NVIDIA driver..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key .github/workflows/scripts/win/install-nvidia-driver.ps1 Administrator@localhost:C:/install_driver.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/install_driver.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Transfer repository to instance
        run: |
          echo "Creating and transferring repository archive..."
          git archive --format=zip --output=/tmp/repo.zip HEAD

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/repo.zip Administrator@localhost:C:/source.zip

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"if (Test-Path 'C:\\ppf-contact-solver') { Remove-Item -Recurse -Force 'C:\\ppf-contact-solver' }; New-Item -ItemType Directory -Path 'C:\\ppf-contact-solver' -Force; Expand-Archive -Path 'C:\\source.zip' -DestinationPath 'C:\\ppf-contact-solver' -Force; Remove-Item 'C:\\source.zip'\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run warmup.bat
        run: |
          echo "Running warmup.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && warmup.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run build.bat
        run: |
          echo "Running build.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && build.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Setup CI directory
        run: |
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"New-Item -ItemType Directory -Path 'C:\\ci' -Force\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run drape
        run: |
          echo "Running drape..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/drape/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_drape.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_drape.ps1 Administrator@localhost:C:/run_drape.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_drape.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run fishingknot
        run: |
          echo "Running fishingknot..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/fishingknot/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_fishingknot.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_fishingknot.ps1 Administrator@localhost:C:/run_fishingknot.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_fishingknot.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run fitting
        run: |
          echo "Running fitting..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/fitting/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_fitting.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_fitting.ps1 Administrator@localhost:C:/run_fitting.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_fitting.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run friction
        run: |
          echo "Running friction..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/friction/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_friction.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_friction.ps1 Administrator@localhost:C:/run_friction.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_friction.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true


      - name: Collect results
        if: success() || failure()
        run: |
          echo "Collecting results..."
          mkdir -p ci

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          # Delete large binary files on remote before copying to save bandwidth
          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"Get-ChildItem -Path C:\\ppf-contact-solver\\cache\\ppf-cts\\ci -Recurse -Include '*.bin','*.pickle','*.ply','*.gz' -ErrorAction SilentlyContinue | Remove-Item -Force\"" || true

          # Copy CI output from ppf-cts cache directory
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ppf-contact-solver/cache/ppf-cts/ci/*" ./ci/ || echo "No ppf-cts CI files found"

          # Also copy logs and scripts from C:\ci
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ci/*" ./ci/ || echo "No script/log files found"

          echo "## Collected Files:"
          ls -laR ci/ || echo "No files collected"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-win-batch-2
          path: ci
          retention-days: 3

      - name: GPU information
        if: success() || failure()
        run: |
          echo "Getting GPU information..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost "nvidia-smi" || echo "Failed to get GPU info"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Re-authenticate for cleanup
        if: always()
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Cleanup - Terminate Instance
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.instance.outputs.INSTANCE_ID }}" ]; then
            echo "Terminating instance: ${{ steps.instance.outputs.INSTANCE_ID }}"
            aws ec2 terminate-instances \
              --instance-ids "${{ steps.instance.outputs.INSTANCE_ID }}" \
              --region "$AWS_REGION" || true
          fi

      - name: Cleanup - Delete Key Pair
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.keypair.outputs.KEY_NAME }}" ]; then
            echo "Deleting key pair: ${{ steps.keypair.outputs.KEY_NAME }}"
            aws ec2 delete-key-pair --key-name "${{ steps.keypair.outputs.KEY_NAME }}" || true
          fi
          rm -f /tmp/ec2key

      - name: Summary
        if: always()
        run: |
          echo "## Batch 2 Summary (Windows)"
          echo "- Examples: drape fishingknot fitting friction"
          echo "- Instance: ${{ steps.instance.outputs.INSTANCE_ID || 'Not launched' }}"

  run-batch-3:
    name: Run Batch 3 (Windows)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: ${{ github.event.inputs.region }}
      INSTANCE_TYPE: ${{ github.event.inputs.instance_type }}
      BRANCH: ${{ github.ref_name }}
      EXAMPLES: "hang needle noodle ribbon"
      WORKDIR: C:\ppf-contact-solver
      USER: Administrator

    steps:
      - name: Show input parameters
        run: |
          echo "## Input Parameters - Batch 3 (Windows)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Instance Type: ${{ github.event.inputs.instance_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Examples: hang needle noodle ribbon"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Find Windows Server AMI and network resources
        id: setup
        run: |
          echo "Finding latest Windows Server 2025 AMI..."
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters \
              "Name=name,Values=Windows_Server-2025-English-Full-Base-*" \
              "Name=state,Values=available" \
            --query 'sort_by(Images, &CreationDate)[-1].ImageId' \
            --region "$AWS_REGION" \
            --output text)

          if [ "$AMI_ID" = "None" ] || [ -z "$AMI_ID" ]; then
            echo "ERROR: Windows Server 2025 AMI not found in region $AWS_REGION"
            exit 1
          fi
          echo "AMI_ID=$AMI_ID" >> $GITHUB_OUTPUT
          echo "Found AMI: $AMI_ID"

          # Get GitHub Actions dedicated VPC, subnet, and security group
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=github-actions-vpc" --query 'Vpcs[0].VpcId' --output text)
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[0].SubnetId' --output text)
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=github-actions-sg" --query 'SecurityGroups[0].GroupId' --output text)

          if [ "$VPC_ID" = "None" ] || [ -z "$VPC_ID" ]; then
            echo "ERROR: github-actions-vpc not found in region $AWS_REGION"
            exit 1
          fi

          echo "::add-mask::$VPC_ID"
          echo "::add-mask::$SUBNET_ID"
          echo "::add-mask::$SG_ID"
          echo "SUBNET_ID=$SUBNET_ID" >> $GITHUB_OUTPUT
          echo "SG_ID=$SG_ID" >> $GITHUB_OUTPUT
          echo "VPC: $VPC_ID, Subnet: $SUBNET_ID, SG: $SG_ID"

      - name: Generate unique identifiers
        id: ids
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "Timestamp: $TIMESTAMP"

      - name: Create EC2 key pair
        id: keypair
        run: |
          KEY_NAME="win-batch-3-${{ steps.ids.outputs.TIMESTAMP }}"
          echo "::add-mask::$KEY_NAME"
          echo "KEY_NAME=$KEY_NAME" >> $GITHUB_OUTPUT

          aws ec2 create-key-pair \
            --key-name "$KEY_NAME" \
            --query 'KeyMaterial' \
            --output text > /tmp/ec2key
          chmod 600 /tmp/ec2key
          echo "Created key pair: $KEY_NAME"

      - name: Create Windows user data script
        run: |
          # Use standard SSH port 22 for EC2 Instance Connect
          sed "s/SSH_PORT_PLACEHOLDER/22/g" .github/workflows/scripts/win/user-data.ps1 > /tmp/user-data.ps1
          echo "User data script created with SSH port 22"

      - name: Launch EC2 instance
        id: instance
        run: |
          echo "Launching Windows EC2 instance..."
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "${{ steps.setup.outputs.AMI_ID }}" \
            --instance-type "$INSTANCE_TYPE" \
            --key-name "${{ steps.keypair.outputs.KEY_NAME }}" \
            --subnet-id "${{ steps.setup.outputs.SUBNET_ID }}" \
            --security-group-ids "${{ steps.setup.outputs.SG_ID }}" \
            --associate-public-ip-address \
            --user-data file:///tmp/user-data.ps1 \
            --block-device-mappings "DeviceName=/dev/sda1,Ebs={VolumeSize=100,VolumeType=gp3,DeleteOnTermination=true}" \
            --tag-specifications \
              "ResourceType=instance,Tags=[{Key=Name,Value=gpu-runner-win-batch-3-${{ steps.ids.outputs.TIMESTAMP }}},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner},{Key=Workflow,Value=${{ github.workflow }}},{Key=RunId,Value=${{ github.run_id }}},{Key=Branch,Value=${{ env.BRANCH }}},{Key=Batch,Value=3}]" \
              "ResourceType=volume,Tags=[{Key=Name,Value=gpu-runner-win-batch-3-${{ steps.ids.outputs.TIMESTAMP }}-volume},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner}]" \
            --instance-initiated-shutdown-behavior terminate \
            --query 'Instances[0].InstanceId' \
            --region "$AWS_REGION" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT
          echo "Instance launched: $INSTANCE_ID"

      - name: Wait for instance ready
        run: |
          INSTANCE_ID="${{ steps.instance.outputs.INSTANCE_ID }}"
          echo "$INSTANCE_ID" > /tmp/instance_id.txt
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids "$INSTANCE_ID"

          echo "Waiting for OpenSSH to be ready via EC2 Instance Connect..."
          for i in {1..30}; do
            timeout 15 aws ec2-instance-connect open-tunnel \
              --instance-id "$INSTANCE_ID" \
              --remote-port 22 \
              --local-port 2222 &
            TUNNEL_PID=$!
            sleep 5

            if nc -z localhost 2222 2>/dev/null; then
              echo "SSH is responding on attempt $i"
              kill $TUNNEL_PID 2>/dev/null || true
              break
            fi

            kill $TUNNEL_PID 2>/dev/null || true
            if [ $i -eq 30 ]; then
              echo "ERROR: SSH not ready after 30 attempts"
              exit 1
            fi
            echo "Attempt $i/30: SSH not ready, waiting 10s..."
            sleep 10
          done
          echo "Instance ready for SSH connections"

      - name: Install NVIDIA driver
        run: |
          echo "Installing NVIDIA driver..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key .github/workflows/scripts/win/install-nvidia-driver.ps1 Administrator@localhost:C:/install_driver.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/install_driver.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Transfer repository to instance
        run: |
          echo "Creating and transferring repository archive..."
          git archive --format=zip --output=/tmp/repo.zip HEAD

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/repo.zip Administrator@localhost:C:/source.zip

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"if (Test-Path 'C:\\ppf-contact-solver') { Remove-Item -Recurse -Force 'C:\\ppf-contact-solver' }; New-Item -ItemType Directory -Path 'C:\\ppf-contact-solver' -Force; Expand-Archive -Path 'C:\\source.zip' -DestinationPath 'C:\\ppf-contact-solver' -Force; Remove-Item 'C:\\source.zip'\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run warmup.bat
        run: |
          echo "Running warmup.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && warmup.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run build.bat
        run: |
          echo "Running build.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && build.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Setup CI directory
        run: |
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"New-Item -ItemType Directory -Path 'C:\\ci' -Force\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run hang
        run: |
          echo "Running hang..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/hang/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_hang.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_hang.ps1 Administrator@localhost:C:/run_hang.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_hang.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run needle
        run: |
          echo "Running needle..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/needle/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_needle.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_needle.ps1 Administrator@localhost:C:/run_needle.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_needle.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run noodle
        run: |
          echo "Running noodle..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/noodle/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_noodle.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_noodle.ps1 Administrator@localhost:C:/run_noodle.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_noodle.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run ribbon
        run: |
          echo "Running ribbon..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/ribbon/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_ribbon.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_ribbon.ps1 Administrator@localhost:C:/run_ribbon.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_ribbon.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true


      - name: Collect results
        if: success() || failure()
        run: |
          echo "Collecting results..."
          mkdir -p ci

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          # Delete large binary files on remote before copying to save bandwidth
          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"Get-ChildItem -Path C:\\ppf-contact-solver\\cache\\ppf-cts\\ci -Recurse -Include '*.bin','*.pickle','*.ply','*.gz' -ErrorAction SilentlyContinue | Remove-Item -Force\"" || true

          # Copy CI output from ppf-cts cache directory
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ppf-contact-solver/cache/ppf-cts/ci/*" ./ci/ || echo "No ppf-cts CI files found"

          # Also copy logs and scripts from C:\ci
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ci/*" ./ci/ || echo "No script/log files found"

          echo "## Collected Files:"
          ls -laR ci/ || echo "No files collected"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-win-batch-3
          path: ci
          retention-days: 3

      - name: GPU information
        if: success() || failure()
        run: |
          echo "Getting GPU information..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost "nvidia-smi" || echo "Failed to get GPU info"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Re-authenticate for cleanup
        if: always()
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Cleanup - Terminate Instance
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.instance.outputs.INSTANCE_ID }}" ]; then
            echo "Terminating instance: ${{ steps.instance.outputs.INSTANCE_ID }}"
            aws ec2 terminate-instances \
              --instance-ids "${{ steps.instance.outputs.INSTANCE_ID }}" \
              --region "$AWS_REGION" || true
          fi

      - name: Cleanup - Delete Key Pair
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.keypair.outputs.KEY_NAME }}" ]; then
            echo "Deleting key pair: ${{ steps.keypair.outputs.KEY_NAME }}"
            aws ec2 delete-key-pair --key-name "${{ steps.keypair.outputs.KEY_NAME }}" || true
          fi
          rm -f /tmp/ec2key

      - name: Summary
        if: always()
        run: |
          echo "## Batch 3 Summary (Windows)"
          echo "- Examples: hang needle noodle ribbon"
          echo "- Instance: ${{ steps.instance.outputs.INSTANCE_ID || 'Not launched' }}"

  run-batch-4:
    name: Run Batch 4 (Windows)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: ${{ github.event.inputs.region }}
      INSTANCE_TYPE: ${{ github.event.inputs.instance_type }}
      BRANCH: ${{ github.ref_name }}
      EXAMPLES: "roller stack trampoline trapped"
      WORKDIR: C:\ppf-contact-solver
      USER: Administrator

    steps:
      - name: Show input parameters
        run: |
          echo "## Input Parameters - Batch 4 (Windows)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Instance Type: ${{ github.event.inputs.instance_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Examples: roller stack trampoline trapped"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Find Windows Server AMI and network resources
        id: setup
        run: |
          echo "Finding latest Windows Server 2025 AMI..."
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters \
              "Name=name,Values=Windows_Server-2025-English-Full-Base-*" \
              "Name=state,Values=available" \
            --query 'sort_by(Images, &CreationDate)[-1].ImageId' \
            --region "$AWS_REGION" \
            --output text)

          if [ "$AMI_ID" = "None" ] || [ -z "$AMI_ID" ]; then
            echo "ERROR: Windows Server 2025 AMI not found in region $AWS_REGION"
            exit 1
          fi
          echo "AMI_ID=$AMI_ID" >> $GITHUB_OUTPUT
          echo "Found AMI: $AMI_ID"

          # Get GitHub Actions dedicated VPC, subnet, and security group
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=github-actions-vpc" --query 'Vpcs[0].VpcId' --output text)
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[0].SubnetId' --output text)
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=github-actions-sg" --query 'SecurityGroups[0].GroupId' --output text)

          if [ "$VPC_ID" = "None" ] || [ -z "$VPC_ID" ]; then
            echo "ERROR: github-actions-vpc not found in region $AWS_REGION"
            exit 1
          fi

          echo "::add-mask::$VPC_ID"
          echo "::add-mask::$SUBNET_ID"
          echo "::add-mask::$SG_ID"
          echo "SUBNET_ID=$SUBNET_ID" >> $GITHUB_OUTPUT
          echo "SG_ID=$SG_ID" >> $GITHUB_OUTPUT
          echo "VPC: $VPC_ID, Subnet: $SUBNET_ID, SG: $SG_ID"

      - name: Generate unique identifiers
        id: ids
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "Timestamp: $TIMESTAMP"

      - name: Create EC2 key pair
        id: keypair
        run: |
          KEY_NAME="win-batch-4-${{ steps.ids.outputs.TIMESTAMP }}"
          echo "::add-mask::$KEY_NAME"
          echo "KEY_NAME=$KEY_NAME" >> $GITHUB_OUTPUT

          aws ec2 create-key-pair \
            --key-name "$KEY_NAME" \
            --query 'KeyMaterial' \
            --output text > /tmp/ec2key
          chmod 600 /tmp/ec2key
          echo "Created key pair: $KEY_NAME"

      - name: Create Windows user data script
        run: |
          # Use standard SSH port 22 for EC2 Instance Connect
          sed "s/SSH_PORT_PLACEHOLDER/22/g" .github/workflows/scripts/win/user-data.ps1 > /tmp/user-data.ps1
          echo "User data script created with SSH port 22"

      - name: Launch EC2 instance
        id: instance
        run: |
          echo "Launching Windows EC2 instance..."
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "${{ steps.setup.outputs.AMI_ID }}" \
            --instance-type "$INSTANCE_TYPE" \
            --key-name "${{ steps.keypair.outputs.KEY_NAME }}" \
            --subnet-id "${{ steps.setup.outputs.SUBNET_ID }}" \
            --security-group-ids "${{ steps.setup.outputs.SG_ID }}" \
            --associate-public-ip-address \
            --user-data file:///tmp/user-data.ps1 \
            --block-device-mappings "DeviceName=/dev/sda1,Ebs={VolumeSize=100,VolumeType=gp3,DeleteOnTermination=true}" \
            --tag-specifications \
              "ResourceType=instance,Tags=[{Key=Name,Value=gpu-runner-win-batch-4-${{ steps.ids.outputs.TIMESTAMP }}},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner},{Key=Workflow,Value=${{ github.workflow }}},{Key=RunId,Value=${{ github.run_id }}},{Key=Branch,Value=${{ env.BRANCH }}},{Key=Batch,Value=4}]" \
              "ResourceType=volume,Tags=[{Key=Name,Value=gpu-runner-win-batch-4-${{ steps.ids.outputs.TIMESTAMP }}-volume},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner}]" \
            --instance-initiated-shutdown-behavior terminate \
            --query 'Instances[0].InstanceId' \
            --region "$AWS_REGION" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT
          echo "Instance launched: $INSTANCE_ID"

      - name: Wait for instance ready
        run: |
          INSTANCE_ID="${{ steps.instance.outputs.INSTANCE_ID }}"
          echo "$INSTANCE_ID" > /tmp/instance_id.txt
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids "$INSTANCE_ID"

          echo "Waiting for OpenSSH to be ready via EC2 Instance Connect..."
          for i in {1..30}; do
            timeout 15 aws ec2-instance-connect open-tunnel \
              --instance-id "$INSTANCE_ID" \
              --remote-port 22 \
              --local-port 2222 &
            TUNNEL_PID=$!
            sleep 5

            if nc -z localhost 2222 2>/dev/null; then
              echo "SSH is responding on attempt $i"
              kill $TUNNEL_PID 2>/dev/null || true
              break
            fi

            kill $TUNNEL_PID 2>/dev/null || true
            if [ $i -eq 30 ]; then
              echo "ERROR: SSH not ready after 30 attempts"
              exit 1
            fi
            echo "Attempt $i/30: SSH not ready, waiting 10s..."
            sleep 10
          done
          echo "Instance ready for SSH connections"

      - name: Install NVIDIA driver
        run: |
          echo "Installing NVIDIA driver..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key .github/workflows/scripts/win/install-nvidia-driver.ps1 Administrator@localhost:C:/install_driver.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/install_driver.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Transfer repository to instance
        run: |
          echo "Creating and transferring repository archive..."
          git archive --format=zip --output=/tmp/repo.zip HEAD

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/repo.zip Administrator@localhost:C:/source.zip

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"if (Test-Path 'C:\\ppf-contact-solver') { Remove-Item -Recurse -Force 'C:\\ppf-contact-solver' }; New-Item -ItemType Directory -Path 'C:\\ppf-contact-solver' -Force; Expand-Archive -Path 'C:\\source.zip' -DestinationPath 'C:\\ppf-contact-solver' -Force; Remove-Item 'C:\\source.zip'\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run warmup.bat
        run: |
          echo "Running warmup.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && warmup.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run build.bat
        run: |
          echo "Running build.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && build.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Setup CI directory
        run: |
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"New-Item -ItemType Directory -Path 'C:\\ci' -Force\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run roller
        run: |
          echo "Running roller..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/roller/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_roller.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_roller.ps1 Administrator@localhost:C:/run_roller.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_roller.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run stack
        run: |
          echo "Running stack..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/stack/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_stack.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_stack.ps1 Administrator@localhost:C:/run_stack.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_stack.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run trampoline
        run: |
          echo "Running trampoline..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/trampoline/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_trampoline.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_trampoline.ps1 Administrator@localhost:C:/run_trampoline.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_trampoline.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run trapped
        run: |
          echo "Running trapped..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/trapped/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_trapped.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_trapped.ps1 Administrator@localhost:C:/run_trapped.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_trapped.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true


      - name: Collect results
        if: success() || failure()
        run: |
          echo "Collecting results..."
          mkdir -p ci

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          # Delete large binary files on remote before copying to save bandwidth
          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"Get-ChildItem -Path C:\\ppf-contact-solver\\cache\\ppf-cts\\ci -Recurse -Include '*.bin','*.pickle','*.ply','*.gz' -ErrorAction SilentlyContinue | Remove-Item -Force\"" || true

          # Copy CI output from ppf-cts cache directory
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ppf-contact-solver/cache/ppf-cts/ci/*" ./ci/ || echo "No ppf-cts CI files found"

          # Also copy logs and scripts from C:\ci
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ci/*" ./ci/ || echo "No script/log files found"

          echo "## Collected Files:"
          ls -laR ci/ || echo "No files collected"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-win-batch-4
          path: ci
          retention-days: 3

      - name: GPU information
        if: success() || failure()
        run: |
          echo "Getting GPU information..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost "nvidia-smi" || echo "Failed to get GPU info"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Re-authenticate for cleanup
        if: always()
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Cleanup - Terminate Instance
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.instance.outputs.INSTANCE_ID }}" ]; then
            echo "Terminating instance: ${{ steps.instance.outputs.INSTANCE_ID }}"
            aws ec2 terminate-instances \
              --instance-ids "${{ steps.instance.outputs.INSTANCE_ID }}" \
              --region "$AWS_REGION" || true
          fi

      - name: Cleanup - Delete Key Pair
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.keypair.outputs.KEY_NAME }}" ]; then
            echo "Deleting key pair: ${{ steps.keypair.outputs.KEY_NAME }}"
            aws ec2 delete-key-pair --key-name "${{ steps.keypair.outputs.KEY_NAME }}" || true
          fi
          rm -f /tmp/ec2key

      - name: Summary
        if: always()
        run: |
          echo "## Batch 4 Summary (Windows)"
          echo "- Examples: roller stack trampoline trapped"
          echo "- Instance: ${{ steps.instance.outputs.INSTANCE_ID || 'Not launched' }}"

  run-batch-5:
    name: Run Batch 5 (Windows)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: ${{ github.event.inputs.region }}
      INSTANCE_TYPE: ${{ github.event.inputs.instance_type }}
      BRANCH: ${{ github.ref_name }}
      EXAMPLES: "twist five-twist woven yarn"
      WORKDIR: C:\ppf-contact-solver
      USER: Administrator

    steps:
      - name: Show input parameters
        run: |
          echo "## Input Parameters - Batch 5 (Windows)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Instance Type: ${{ github.event.inputs.instance_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Examples: twist five-twist woven yarn"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Find Windows Server AMI and network resources
        id: setup
        run: |
          echo "Finding latest Windows Server 2025 AMI..."
          AMI_ID=$(aws ec2 describe-images \
            --owners amazon \
            --filters \
              "Name=name,Values=Windows_Server-2025-English-Full-Base-*" \
              "Name=state,Values=available" \
            --query 'sort_by(Images, &CreationDate)[-1].ImageId' \
            --region "$AWS_REGION" \
            --output text)

          if [ "$AMI_ID" = "None" ] || [ -z "$AMI_ID" ]; then
            echo "ERROR: Windows Server 2025 AMI not found in region $AWS_REGION"
            exit 1
          fi
          echo "AMI_ID=$AMI_ID" >> $GITHUB_OUTPUT
          echo "Found AMI: $AMI_ID"

          # Get GitHub Actions dedicated VPC, subnet, and security group
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=github-actions-vpc" --query 'Vpcs[0].VpcId' --output text)
          SUBNET_ID=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[0].SubnetId' --output text)
          SG_ID=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=github-actions-sg" --query 'SecurityGroups[0].GroupId' --output text)

          if [ "$VPC_ID" = "None" ] || [ -z "$VPC_ID" ]; then
            echo "ERROR: github-actions-vpc not found in region $AWS_REGION"
            exit 1
          fi

          echo "::add-mask::$VPC_ID"
          echo "::add-mask::$SUBNET_ID"
          echo "::add-mask::$SG_ID"
          echo "SUBNET_ID=$SUBNET_ID" >> $GITHUB_OUTPUT
          echo "SG_ID=$SG_ID" >> $GITHUB_OUTPUT
          echo "VPC: $VPC_ID, Subnet: $SUBNET_ID, SG: $SG_ID"

      - name: Generate unique identifiers
        id: ids
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          RANDOM_SUFFIX=$(head /dev/urandom | tr -dc a-z0-9 | head -c 6)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "Timestamp: $TIMESTAMP"

      - name: Create EC2 key pair
        id: keypair
        run: |
          KEY_NAME="win-batch-5-${{ steps.ids.outputs.TIMESTAMP }}"
          echo "::add-mask::$KEY_NAME"
          echo "KEY_NAME=$KEY_NAME" >> $GITHUB_OUTPUT

          aws ec2 create-key-pair \
            --key-name "$KEY_NAME" \
            --query 'KeyMaterial' \
            --output text > /tmp/ec2key
          chmod 600 /tmp/ec2key
          echo "Created key pair: $KEY_NAME"

      - name: Create Windows user data script
        run: |
          # Use standard SSH port 22 for EC2 Instance Connect
          sed "s/SSH_PORT_PLACEHOLDER/22/g" .github/workflows/scripts/win/user-data.ps1 > /tmp/user-data.ps1
          echo "User data script created with SSH port 22"

      - name: Launch EC2 instance
        id: instance
        run: |
          echo "Launching Windows EC2 instance..."
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "${{ steps.setup.outputs.AMI_ID }}" \
            --instance-type "$INSTANCE_TYPE" \
            --key-name "${{ steps.keypair.outputs.KEY_NAME }}" \
            --subnet-id "${{ steps.setup.outputs.SUBNET_ID }}" \
            --security-group-ids "${{ steps.setup.outputs.SG_ID }}" \
            --associate-public-ip-address \
            --user-data file:///tmp/user-data.ps1 \
            --block-device-mappings "DeviceName=/dev/sda1,Ebs={VolumeSize=100,VolumeType=gp3,DeleteOnTermination=true}" \
            --tag-specifications \
              "ResourceType=instance,Tags=[{Key=Name,Value=gpu-runner-win-batch-5-${{ steps.ids.outputs.TIMESTAMP }}},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner},{Key=Workflow,Value=${{ github.workflow }}},{Key=RunId,Value=${{ github.run_id }}},{Key=Branch,Value=${{ env.BRANCH }}},{Key=Batch,Value=5}]" \
              "ResourceType=volume,Tags=[{Key=Name,Value=gpu-runner-win-batch-5-${{ steps.ids.outputs.TIMESTAMP }}-volume},{Key=ManagedBy,Value=GitHubActions},{Key=Purpose,Value=WindowsGPURunner}]" \
            --instance-initiated-shutdown-behavior terminate \
            --query 'Instances[0].InstanceId' \
            --region "$AWS_REGION" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT
          echo "Instance launched: $INSTANCE_ID"

      - name: Wait for instance ready
        run: |
          INSTANCE_ID="${{ steps.instance.outputs.INSTANCE_ID }}"
          echo "$INSTANCE_ID" > /tmp/instance_id.txt
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids "$INSTANCE_ID"

          echo "Waiting for OpenSSH to be ready via EC2 Instance Connect..."
          for i in {1..30}; do
            timeout 15 aws ec2-instance-connect open-tunnel \
              --instance-id "$INSTANCE_ID" \
              --remote-port 22 \
              --local-port 2222 &
            TUNNEL_PID=$!
            sleep 5

            if nc -z localhost 2222 2>/dev/null; then
              echo "SSH is responding on attempt $i"
              kill $TUNNEL_PID 2>/dev/null || true
              break
            fi

            kill $TUNNEL_PID 2>/dev/null || true
            if [ $i -eq 30 ]; then
              echo "ERROR: SSH not ready after 30 attempts"
              exit 1
            fi
            echo "Attempt $i/30: SSH not ready, waiting 10s..."
            sleep 10
          done
          echo "Instance ready for SSH connections"

      - name: Install NVIDIA driver
        run: |
          echo "Installing NVIDIA driver..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key .github/workflows/scripts/win/install-nvidia-driver.ps1 Administrator@localhost:C:/install_driver.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/install_driver.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Transfer repository to instance
        run: |
          echo "Creating and transferring repository archive..."
          git archive --format=zip --output=/tmp/repo.zip HEAD

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/repo.zip Administrator@localhost:C:/source.zip

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"if (Test-Path 'C:\\ppf-contact-solver') { Remove-Item -Recurse -Force 'C:\\ppf-contact-solver' }; New-Item -ItemType Directory -Path 'C:\\ppf-contact-solver' -Force; Expand-Archive -Path 'C:\\source.zip' -DestinationPath 'C:\\ppf-contact-solver' -Force; Remove-Item 'C:\\source.zip'\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run warmup.bat
        run: |
          echo "Running warmup.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && warmup.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run build.bat
        run: |
          echo "Running build.bat..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "cmd /c 'cd C:\\ppf-contact-solver\\build-win-native && build.bat /nopause'"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Setup CI directory
        run: |
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"New-Item -ItemType Directory -Path 'C:\\ci' -Force\""

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run twist
        run: |
          echo "Running twist..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/twist/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_twist.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_twist.ps1 Administrator@localhost:C:/run_twist.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_twist.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run five-twist
        run: |
          echo "Running five-twist..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/five-twist/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_five-twist.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_five-twist.ps1 Administrator@localhost:C:/run_five-twist.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_five-twist.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run woven
        run: |
          echo "Running woven..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/woven/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_woven.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_woven.ps1 Administrator@localhost:C:/run_woven.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_woven.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Run yarn
        run: |
          echo "Running yarn..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          sed "s/EXAMPLE_PLACEHOLDER/yarn/g" .github/workflows/scripts/win/run-example.ps1 > /tmp/run_yarn.ps1

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          scp -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key /tmp/run_yarn.ps1 Administrator@localhost:C:/run_yarn.ps1

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -ExecutionPolicy Bypass -File C:/run_yarn.ps1"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true


      - name: Collect results
        if: success() || failure()
        run: |
          echo "Collecting results..."
          mkdir -p ci

          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          # Delete large binary files on remote before copying to save bandwidth
          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost \
            "powershell -Command \"Get-ChildItem -Path C:\\ppf-contact-solver\\cache\\ppf-cts\\ci -Recurse -Include '*.bin','*.pickle','*.ply','*.gz' -ErrorAction SilentlyContinue | Remove-Item -Force\"" || true

          # Copy CI output from ppf-cts cache directory
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ppf-contact-solver/cache/ppf-cts/ci/*" ./ci/ || echo "No ppf-cts CI files found"

          # Also copy logs and scripts from C:\ci
          scp -P 2222 -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key "Administrator@localhost:C:/ci/*" ./ci/ || echo "No script/log files found"

          echo "## Collected Files:"
          ls -laR ci/ || echo "No files collected"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-win-batch-5
          path: ci
          retention-days: 3

      - name: GPU information
        if: success() || failure()
        run: |
          echo "Getting GPU information..."
          INSTANCE_ID=$(cat /tmp/instance_id.txt)

          # Open tunnel for this step
          aws ec2-instance-connect open-tunnel \
            --instance-id "$INSTANCE_ID" \
            --remote-port 22 \
            --local-port 2222 &
          TUNNEL_PID=$!
          sleep 5

          ssh -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -o ServerAliveInterval=60 -o ServerAliveCountMax=10 \
            -i /tmp/ec2key Administrator@localhost "nvidia-smi" || echo "Failed to get GPU info"

          # Close tunnel
          kill $TUNNEL_PID 2>/dev/null || true

      - name: Re-authenticate for cleanup
        if: always()
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Cleanup - Terminate Instance
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.instance.outputs.INSTANCE_ID }}" ]; then
            echo "Terminating instance: ${{ steps.instance.outputs.INSTANCE_ID }}"
            aws ec2 terminate-instances \
              --instance-ids "${{ steps.instance.outputs.INSTANCE_ID }}" \
              --region "$AWS_REGION" || true
          fi

      - name: Cleanup - Delete Key Pair
        if: always()
        continue-on-error: true
        run: |
          if [ -n "${{ steps.keypair.outputs.KEY_NAME }}" ]; then
            echo "Deleting key pair: ${{ steps.keypair.outputs.KEY_NAME }}"
            aws ec2 delete-key-pair --key-name "${{ steps.keypair.outputs.KEY_NAME }}" || true
          fi
          rm -f /tmp/ec2key

      - name: Summary
        if: always()
        run: |
          echo "## Batch 5 Summary (Windows)"
          echo "- Examples: twist five-twist woven yarn"
          echo "- Instance: ${{ steps.instance.outputs.INSTANCE_ID || 'Not launched' }}"

